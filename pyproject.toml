[tool.uv]
package = false

[project]
name = "data-engineering-zoomcamp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "mpuust", email = "marten.puust@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "pandas>=3.0.5",
    "pyarrow>=25.0.1",
]

[dependency-groups]
dev = [
    "pgcli>=4.5.0",
]
